# WeCom 分步交互

仅在企业微信网关执行报价任务时读取。

WeCom 用户在一次回复中通常看不到工具调用期间的中间进度。fetch、整理、生成与验证可能持续较长时间，因此按阶段分别回复：

1. `🔍 正在查询 N 个服务...` → fetch
2. `✅ 查询完成` → 列出服务名、数量、价格和币种
3. `📝 正在整理数据、生成报价单...` → validate + build + verify
4. `✅ 报价单已生成` → 附结果与金额摘要

不要在进度消息中展示原始 JSON、内部配置或未经验证的最终金额。
